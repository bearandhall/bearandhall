name = "gom-hwae-dang"
compatibility_date = "2024-01-01"

[vars]
# 콤마로 구분한 허용 이메일(또는 키). 운영에서는 대체로 DB/KV 사용 권장
ALLOWED_EMAILS = "you@example.com,subscriber@site.com"
# 토큰 서명용 비밀키 (랜덤 긴 문자열)
SIGNING_SECRET = "change-this-to-a-long-random-secret"
# 토큰 유효기간(초) – 1년
TOKEN_TTL = "31536000"
